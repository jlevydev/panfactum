import type { FastifyPluginAsync } from 'fastify'

export const AdminRoutes: FastifyPluginAsync = async (_) => {
  // const prefix = '/org/:orgId'
  // await fastify.register(GetPackagesRoute, { prefix })
}
